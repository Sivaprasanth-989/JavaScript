let person = {
    name : "Chirdeep",
    age : 28,
};
// Object destructuring for accesing multiple keys unpacking values of it 
let {name, age} = person;
console.log(name);
console.log(person.name);
